# Screenshots

The README's "Desktop & mobile" section references four images. Drop PNGs here at these exact paths:

- `desktop-browse.png` — desktop browser, browse globe with live contacts
- `desktop-cockpit.png` — desktop browser, first-person cockpit while flying (instrument panel visible)
- `mobile-browse.png` — phone/tablet, browse view
- `mobile-cockpit.png` — phone/tablet, flying with the touch rich HUD

Tips: landscape PNGs, ~1600px wide is plenty; keep each well under a few hundred KB so the
repo stays light. GitHub renders them inline in the README.
